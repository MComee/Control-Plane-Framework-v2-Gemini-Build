# Tasks

Store one file per bounded task.

Rules:
- Use `framework/templates/task-template.md`.
- Each task must define allowed scope and forbidden scope.
- Status must be synchronized with priorities and `project/now/`.
