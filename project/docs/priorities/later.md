# Later

- Define additional project-specific feature and task decomposition under `project/docs/features/`, `project/docs/task_groups/`, and `project/docs/tasks/`
- Add lightweight automation for file-tree compliance checks if manual maintenance becomes error-prone
