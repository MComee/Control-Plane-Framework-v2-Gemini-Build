# Active Work Description

Current work is `TASK-002`: enforce exact repository visibility by keeping `README.md` synchronized to the full tracked file tree and removing stale path references from governance documents.

This work is operational control work, not product-feature implementation. It protects repository truth so different tools can execute against the same stable state.
